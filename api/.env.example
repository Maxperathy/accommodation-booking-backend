# ============================================
# Server Configuration
# ============================================
# Port number for the server to listen on
PORT=3000

# Environment mode: development, production, or test
NODE_ENV=development

# ============================================
# MongoDB Configuration
# ============================================
# Local MongoDB connection string
# MONGO_URI=mongodb://localhost:27017/accomBook

# MongoDB Atlas connection string (for cloud database)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/accomBook?retryWrites=true&w=majority

# Replace with your actual MongoDB connection string
MONGO_URI=mongodb://localhost:27017/accomBook

# ============================================
# JWT Configuration
# ============================================
# Secret key for signing access tokens
# IMPORTANT: Use a strong, random string in production!
# Generate using: openssl rand -base64 32
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-this-in-production

# Secret key for signing refresh tokens
# IMPORTANT: Use a different strong, random string in production!
# Generate using: openssl rand -base64 32
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-this-in-production

# Access token expiration time
# Format: number followed by unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 15m, 1h, 24h
ACCESS_TOKEN_EXPIRY=15m

# Refresh token expiration time
# Format: number followed by unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 7d, 30d, 90d
REFRESH_TOKEN_EXPIRY=7d

# ============================================
# Cloudinary Configuration
# ============================================
# Get these values from your Cloudinary dashboard: https://cloudinary.com/console
# Cloudinary cloud name
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name

# Cloudinary API Key
CLOUDINARY_API_KEY=your-cloudinary-api-key

# Cloudinary API Secret
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins for CORS
# In development mode, all origins are allowed
# In production, specify your frontend domain(s)
# Examples:
# WHITELIST_ORIGINS=https://yourdomain.com
# WHITELIST_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
WHITELIST_ORIGINS=https://your-frontend-domain.com

# ============================================
# Logging Configuration
# ============================================
# Log level: error, warn, info, debug
# Recommended: 'debug' for development, 'info' or 'warn' for production
LOG_LEVEL=info
